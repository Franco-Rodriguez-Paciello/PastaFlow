# PastaFlow Client

Frontend React + Vite del ERP PastaFlow.

Instrucciones de instalación, credenciales de demo y recorrido para recruiters: ver [README principal](../README.md).

```bash
npm install
npm run dev
```

La app corre en `http://localhost:5173` y proxea `/api` hacia `http://localhost:5095`.
